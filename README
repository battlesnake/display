Syntax:
	gamma <value>
	brightness <value>
	
`gamma` sets the gamma for all connected displays to the given value.
`brightness` sets only the brightness for all connected displays to the given value.

Install to $PATH (e.g. /usr/bin on Arch) to allow easy access via shell / "run bar"

Requires:
	xrandr
